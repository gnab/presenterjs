div.panel(data-bind= "template: { name: 'panelTemplate', data: panel }")
div.splitter
div#presenter
script
  presenter.onready(function () {
    var file = '#{file}';
    var data = '#{data}';
    new presenter.viewmodels.Presenter(file, data);
  });
